{
  "id": "openclaw-soundcloud-plugin",
  "name": "SoundCloud download plugin",
  "description": "Registers a soundcloud tool that downloads a SoundCloud URL with soundcloud-dl and returns the downloaded file path.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "url": {
        "type": "string",
        "description": "Track or playlist URL on the Soundcloud"
      },
      "downloadPath": {
        "type": "string",
        "default": "/tmp/openclaw/plugins/openclaw-soundcloud-plugin",
        "description": "Flag used by soundcloud-dl to choose where media files are stored."
      },
      "force": {
        "type": "boolean",
        "default": false,
        "description": "Allows to overwrite an existing file in the target directory."
      },
      "socksProxy": {
        "type": "string",
        "default": "",
        "description": "Passes to the downloader when a SOCKS5 proxy is required."
      }
    }
  },
  "commandAliases": [
    {
      "name": "sound",
      "kind": "runtime-slash",
      "cliCommand": "memory"
    }
  ]
}
