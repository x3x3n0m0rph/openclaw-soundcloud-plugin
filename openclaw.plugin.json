{
  "id": "openclaw-soundcloud-plugin",
  "name": "SoundCloud download plugin",
  "description": "Registers a soundcloud tool that downloads a SoundCloud URL with soundcloud-dl and returns the downloaded file path.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "downloadFolder": {
        "type": "string",
        "description": "Directory where each download session creates a temporary subfolder."
      },
      "downloadPath": {
        "type": "string",
        "description": "Deprecated alias for downloadFolder (same meaning)."
      },
      "force": {
        "type": "boolean",
        "default": false,
        "description": "Allows to overwrite an existing file in the target directory."
      },
      "socksProxy": {
        "type": "string",
        "default": "",
        "description": "Passes to the downloader when a SOCKS5 proxy is required."
      },
      "timeoutSeconds": {
        "type": "number",
        "default": 600,
        "description": "Maximum time to wait for the downloader process."
      },
      "allowedHosts": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Hostname allowlist for the SoundCloud URL (subdomains allowed)."
      }
    }
  }
}
